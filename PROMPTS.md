# Smart Campus Planner — PROMPTS (단계별 구현 요청 프롬프트)

본 문서는 `PRD.md`(프로젝트 목적/요구사항/MVP)와 `TASKS.md`(T1~T3 단계별 계획)를 바탕으로, 실제 구현을 요청할 때 그대로 사용할 수 있는 프롬프트 모음입니다.

## 사용 방법
1. 아래 **T1 프롬프트**를 먼저 AI 코딩 에이전트(Cline 등)에게 그대로 전달해 구현을 요청합니다.
2. 결과물을 직접 실행/확인하고, 이상이 없으면 **T2 프롬프트**를 전달합니다.
3. 마찬가지로 T2 결과를 확인한 뒤 **T3 프롬프트**를 전달합니다.
4. 한 번에 여러 단계를 몰아서 요청하지 않고, 반드시 이전 단계 확인 후 다음 단계로 넘어갑니다.

> ⚠️ 본 문서는 프롬프트 정리 목적이며, 이 시점에서는 실제 프로그램 코드를 작성하거나 수정하지 않습니다. 아래 프롬프트들은 "다음에 AI에게 그대로 전달할 요청 문구"입니다.

---

## T1 프롬프트 — 할 일 추가·목록·삭제 (기본 틀 만들기)

```
현재 프로젝트 폴더에 있는 PRD.md와 TASKS.md 파일을 참고해서,
TASKS.md의 1단계(T1)에 해당하는 기능만 구현해줘.

이번 단계에서 만들 것은 "할 일 관리 웹앱의 가장 기본적인 틀"이야.
- 할 일 제목을 입력하고 버튼을 눌러 목록에 새 항목을 추가할 수 있어야 해.
- 추가된 할 일들이 화면에 목록 형태로 보여야 해.
- 목록에서 원하는 할 일을 선택해서 삭제할 수 있어야 해.
- 할 일이 하나도 없을 때와 여러 개 있을 때 모두 화면이 자연스럽게 보여야 해.

주의할 점:
- 카테고리(학과/아르바이트/개인), 중요도(상/중/하), 마감일, 완료 체크, 완료율, 새로고침 후 데이터 유지 기능은
  다음 단계(T2, T3)에서 다룰 예정이니 이번 단계에서는 구현하지 마.
- 특별한 요청이 없다면 별도의 복잡한 도구 설치 없이, 웹 브라우저에서 바로 열어볼 수 있는
  간단한 웹페이지(HTML/CSS/JavaScript) 형태로 만들어줘.
- 코드는 이해하기 쉽게 파일과 폴더를 정리해줘.
- 구현이 끝나면 내가 직접 브라우저로 열어서 "할 일 추가/목록 확인/삭제"가 잘 되는지
  확인할 수 있도록, 어떻게 실행하면 되는지도 알려줘.

이번 요청에서는 1단계 범위까지만 구현하고, 2단계·3단계 기능은 아직 만들지 마.
```

**T1 결과를 확인하는 방법 (체크리스트)**
- [ ] 할 일 제목을 입력하고 추가하면 목록에 바로 나타나는가?
- [ ] 목록에서 특정 항목을 삭제하면 화면에서 사라지는가?
- [ ] 할 일이 하나도 없을 때 화면이 깨지지 않고 자연스럽게 보이는가?

위 항목이 모두 정상이면 T2 프롬프트로 넘어갑니다.

---

## T2 프롬프트 — 카테고리·중요도·마감일·완료 체크·완료율 (핵심 기능 채우기)

```
현재 프로젝트 폴더에 있는 PRD.md와 TASKS.md 파일을 참고해서,
TASKS.md의 2단계(T2)에 해당하는 기능을 이전에 만든 1단계 결과물 위에 추가로 구현해줘.

이번 단계에서 추가할 기능은 다음과 같아.
- 할 일을 추가할 때 카테고리를 "학과 / 아르바이트 / 개인" 중 하나로 선택할 수 있어야 해.
- 할 일을 추가할 때 중요도를 "상 / 중 / 하" 중 하나로 선택할 수 있어야 해.
- 할 일을 추가할 때 마감일을 날짜로 지정할 수 있어야 해.
- 목록에서 각 할 일의 카테고리, 중요도, 마감일이 함께 보이도록 화면을 보완해줘.
- 각 할 일을 완료로 체크하거나 다시 미완료로 되돌릴 수 있어야 해. (완료된 항목은 시각적으로 구분되면 좋겠어)
- 전체 할 일 개수 대비 완료된 할 일 개수를 계산해서 완료율(%)을 화면 어딘가에 보여줘야 해.
  (예: "완료율: 40% (2/5)" 같은 형태)

주의할 점:
- 1단계에서 만든 "추가/목록/삭제" 기능은 그대로 유지하면서 위 기능들을 자연스럽게 이어붙여줘.
- 새로고침해도 데이터가 유지되는 저장 기능은 다음 단계(T3)에서 다룰 예정이니 이번에는 구현하지 마.
- 구현이 끝나면 내가 직접 확인할 수 있도록, 어떻게 실행하고 어떤 항목을 눌러보면 되는지 알려줘.

이번 요청에서는 2단계 범위까지만 구현하고, 3단계(저장) 기능은 아직 만들지 마.
```

**T2 결과를 확인하는 방법 (체크리스트)**
- [ ] 할 일을 추가할 때 카테고리(학과/아르바이트/개인)를 선택할 수 있는가?
- [ ] 할 일을 추가할 때 중요도(상/중/하)를 선택할 수 있는가?
- [ ] 할 일을 추가할 때 마감일(날짜)을 지정할 수 있는가?
- [ ] 목록에서 각 항목의 카테고리/중요도/마감일이 보이는가?
- [ ] 할 일을 완료로 체크하거나 다시 해제할 수 있는가?
- [ ] 완료 개수/전체 개수를 기준으로 완료율(%)이 화면에 표시되고, 체크할 때마다 값이 바뀌는가?

위 항목이 모두 정상이면 T3 프롬프트로 넘어갑니다.

---

## T3 프롬프트 — 새로고침해도 유지되는 저장 기능 (데이터 지속성)

```
현재 프로젝트 폴더에 있는 PRD.md와 TASKS.md 파일을 참고해서,
TASKS.md의 3단계(T3)에 해당하는 기능을 이전에 만든 1~2단계 결과물 위에 추가로 구현해줘.

이번 단계에서 추가할 기능은 다음과 같아.
- 할 일을 추가하거나, 완료 체크를 하거나, 삭제할 때마다 현재 할 일 목록 전체(제목, 카테고리, 중요도,
  마감일, 완료 여부 포함)가 자동으로 저장되어야 해.
- 브라우저를 새로고침하거나 다시 접속했을 때, 저장되어 있던 할 일 목록이 그대로 화면에 복원되어야 해.
- 아직 아무 할 일도 등록한 적이 없는 최초 상태에서도 오류 없이 정상적으로 빈 화면이 보여야 해.
- 새로고침 후에도 완료율(%) 표시가 저장된 데이터를 기준으로 정확하게 다시 계산되어야 해.

주의할 점:
- 별도의 서버나 회원가입 없이, 이 웹앱만으로 데이터가 유지되는 방식으로 구현해줘.
  (예: 브라우저 자체에 데이터를 보관하는 방식 등, 가장 간단하고 안정적인 방법을 사용해줘)
- 1~2단계에서 만든 추가/삭제/카테고리/중요도/마감일/완료 체크/완료율 기능은 모두 그대로 동작해야 해.
- 구현이 끝나면 내가 직접 "할 일을 추가한 뒤 새로고침해도 남아있는지"를 확인할 수 있도록,
  확인 방법을 알려줘.

이번 요청을 끝으로 PRD.md에 정의된 MVP 범위(할 일 추가, 카테고리/중요도/마감일 설정,
완료 체크 및 삭제, 전체 완료율 표시, 새로고침해도 유지되는 저장)가 모두 구현되어야 해.
```

**T3 결과를 확인하는 방법 (체크리스트)**
- [ ] 할 일을 몇 개 추가한 뒤 브라우저를 새로고침해도 목록이 그대로 남아있는가?
- [ ] 완료 체크한 항목이 새로고침 후에도 완료 상태로 유지되는가?
- [ ] 삭제한 항목이 새로고침 후에도 다시 나타나지 않는가?
- [ ] 새로고침 후 완료율(%)이 저장된 데이터 기준으로 올바르게 표시되는가?
- [ ] 할 일이 하나도 없는 최초 상태(또는 모두 삭제한 상태)에서 새로고침해도 오류 없이 정상적으로 보이는가?

위 항목이 모두 정상이면 PRD.md 7장의 "성공 기준"을 만족하는 MVP가 완성된 것입니다.

---

## 참고 — 단계 진행 원칙
- 각 프롬프트는 반드시 **한 번에 하나씩** 순서대로(T1 → T2 → T3) 사용합니다.
- 이전 단계의 체크리스트가 모두 통과되기 전에는 다음 단계 프롬프트를 사용하지 않습니다.
- 만약 특정 단계에서 결과가 기대와 다르면, 같은 단계 프롬프트를 기반으로 "어떤 부분이 다른지"를 구체적으로 덧붙여 다시 요청합니다.
