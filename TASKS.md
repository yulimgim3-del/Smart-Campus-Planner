# Smart Campus Planner — TASKS (단계별 구현 계획)

본 문서는 `PRD.md`에 정의된 요구사항과 MVP 범위를 실제로 구현하기 위해, 전체 작업을 **3단계**로 나눈 계획서입니다.
각 단계는 이전 단계에서 만든 결과물을 바탕으로 다음 기능을 쌓아 올리는 방식으로 진행됩니다.

> ⚠️ 본 문서는 계획 정리 목적이며, 이 시점에서는 실제 프로그램 코드를 작성하거나 수정하지 않습니다.

---

## 전체 흐름 요약

| 단계 | 한 줄 요약 | 완료 시 사용자가 할 수 있는 것 |
|------|-----------|-------------------------------|
| 1단계 | 할 일을 적고, 보고, 지우는 기본 틀 만들기 | 할 일을 입력해서 추가하고, 목록에서 확인하고, 필요 없는 항목을 지울 수 있다 |
| 2단계 | 할 일에 세부 정보(카테고리/중요도/마감일)와 완료 체크, 완료율 붙이기 | 할 일마다 분류·중요도·마감일을 정하고, 완료 체크를 하면 전체 완료율이 자동으로 계산되어 보인다 |
| 3단계 | 새로고침해도 사라지지 않게 저장하기 | 브라우저를 새로고침하거나 다시 열어도 이전에 등록한 할 일과 완료 상태가 그대로 남아 있다 |

---

## 1단계 — 할 일 추가·목록·삭제 (기본 틀 만들기)

### 목표
가장 먼저, "할 일을 적고 → 화면에서 확인하고 → 필요 없으면 지운다"는 앱의 가장 기본적인 틀을 만듭니다. 이 단계에서는 카테고리, 중요도, 마감일 같은 세부 정보는 아직 다루지 않고, 할 일의 제목만 다룹니다.

### 비전공자를 위한 설명
빈 종이 위에 "해야 할 일 목록"을 적을 수 있는 메모장을 만드는 단계라고 생각하면 됩니다. 아직 색깔을 칠하거나(카테고리), 별표를 매기거나(중요도), 날짜를 적지는(마감일) 않지만, 줄을 추가하고 지울 수 있는 메모장 자체는 완성되는 단계입니다.

### 이 단계에서 구현할 내용
- 할 일 제목을 입력하고 "추가" 버튼(또는 이에 준하는 동작)으로 목록에 새 항목을 추가하는 기능
- 추가된 할 일들이 목록 형태로 화면에 표시되는 기능
- 목록에서 원하는 할 일을 선택해 삭제하는 기능
- 할 일이 하나도 없을 때와 여러 개 있을 때 모두 화면이 자연스럽게 보이도록 기본 화면 구성

### 관련 PRD 요구사항
- FR-1 (할 일 추가) — 제목 입력/추가 기능
- FR-6 (삭제)

### 이 단계가 끝나면
사용자는 할 일 제목만으로 목록을 관리할 수 있는, 아주 단순하지만 동작하는 할 일 앱을 사용할 수 있습니다. 아직 카테고리 구분, 중요도, 마감일, 완료 체크, 저장 기능은 없는 상태입니다.

---

## 2단계 — 카테고리·중요도·마감일·완료 체크·완료율 (핵심 기능 채우기)

### 목표
1단계에서 만든 "할 일 목록 틀" 위에, 대학생 일정 관리에 꼭 필요한 세부 정보(카테고리, 중요도, 마감일)와 완료 체크 기능, 그리고 전체 완료율 표시 기능을 추가합니다.

### 비전공자를 위한 설명
1단계에서 만든 메모장에 이제 항목별로 "이건 학과 일, 이건 아르바이트, 이건 개인 일"이라는 꼬리표(카테고리)를 붙이고, "이건 중요, 이건 보통, 이건 덜 중요"라는 별표(중요도)를 매기고, "언제까지 해야 하는지" 마감일을 적어 넣습니다. 또한 할 일을 끝내면 체크 표시를 할 수 있게 하고, 전체 중 몇 %를 끝냈는지 한눈에 보여주는 진행률 표시도 함께 만듭니다.

### 이 단계에서 구현할 내용
- 할 일을 추가할 때(또는 등록 후에) 카테고리를 학과 / 아르바이트 / 개인 중에서 선택하는 기능
- 할 일을 추가할 때(또는 등록 후에) 중요도를 상 / 중 / 하 중에서 선택하는 기능
- 할 일을 추가할 때(또는 등록 후에) 마감일을 날짜로 지정하는 기능
- 목록에서 카테고리, 중요도, 마감일 정보가 함께 보이도록 화면 구성 보완
- 할 일을 완료로 체크하거나 다시 미완료로 되돌리는 기능
- 전체 할 일 개수 대비 완료된 할 일 개수를 계산해 완료율(%)로 화면에 표시하는 기능

### 관련 PRD 요구사항
- FR-1 (할 일 추가 — 세부 정보 포함하여 완성)
- FR-2 (카테고리 설정)
- FR-3 (중요도 설정)
- FR-4 (마감일 지정)
- FR-5 (완료 체크)
- FR-7 (전체 완료율 표시)

### 이 단계가 끝나면
사용자는 PRD에서 정의한 핵심 기능(추가, 분류, 중요도, 마감일, 완료 체크, 완료율)을 모두 사용할 수 있는 상태가 됩니다. 다만 아직 새로고침하면 입력했던 내용이 모두 사라지는 상태입니다.

---

## 3단계 — 새로고침해도 유지되는 저장 기능 (데이터 지속성)

### 목표
지금까지 만든 모든 기능(할 일, 카테고리, 중요도, 마감일, 완료 여부)이 새로고침하거나 브라우저를 껐다 켜도 사라지지 않고 그대로 남아 있도록 저장 기능을 연결합니다.

### 비전공자를 위한 설명
지금까지는 메모장에 적은 내용이 창을 닫으면 사라지는 상태였습니다. 이 단계에서는 적은 내용을 자동으로 "저장함"에 보관해 두었다가, 다시 메모장을 열었을 때 저장해 둔 내용을 그대로 불러오게 만드는 작업입니다. 사용자 입장에서는 특별히 "저장" 버튼을 누르지 않아도, 할 일을 추가하거나 체크하거나 지울 때마다 자동으로 안전하게 보관되는 느낌을 받게 됩니다.

### 이 단계에서 구현할 내용
- 할 일을 추가/수정(완료 체크 등)/삭제할 때마다 현재 목록 전체를 저장하는 기능
- 앱을 처음 열거나 새로고침했을 때, 저장되어 있던 목록을 불러와 화면에 그대로 복원하는 기능
- 저장된 데이터가 없는 최초 실행 상태(할 일이 하나도 없는 경우)도 문제없이 동작하도록 처리
- 완료율 표시 등 2단계에서 만든 화면 요소가 새로고침 후에도 저장된 데이터를 기준으로 정확히 다시 계산되는지 확인

### 관련 PRD 요구사항
- FR-8 (데이터 유지 / 영속성)

### 이 단계가 끝나면
PRD에 정의된 MVP 범위(할 일 추가, 카테고리/중요도/마감일 설정, 완료 체크 및 삭제, 전체 완료율 표시, 새로고침해도 유지되는 저장)가 모두 구현되어, PRD 7장의 "성공 기준"을 만족하는 완성된 MVP 상태가 됩니다.

---

## 단계 간 연결 관계 요약

```
[1단계] 추가 · 목록 · 삭제 (기본 틀)
        │  (이 틀 위에 세부 정보와 상태를 붙임)
        ▼
[2단계] 카테고리 · 중요도 · 마감일 · 완료 체크 · 완료율 (핵심 기능)
        │  (이 상태를 화면이 꺼져도 잃지 않도록 보관)
        ▼
[3단계] 새로고침해도 유지되는 저장 (데이터 지속성)
```

각 단계는 독립적으로 눈으로 확인 가능한 결과물을 만들어내며, 다음 단계는 항상 이전 단계에서 만든 화면과 기능을 그대로 이어받아 확장하는 방식으로 진행됩니다.
